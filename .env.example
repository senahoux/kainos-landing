# Kainós Landing — Environment Variables
# Copy this to .env.local for local development
# Set all of these in the Vercel dashboard for the landing project

# Supabase (same project as the app)
VITE_SUPABASE_URL=https://soufwfuioshgtyztropy.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Stripe (server-side only — NEVER expose secret key to frontend)
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_live_webhook_secret_here

# Supabase service role (server-side only — used by webhook to bypass RLS)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Stripe Price IDs LIVE — official production prices
STRIPE_PRICE_ID_MONTHLY=price_1T31owLdVnAwm3JUlovYdaik
STRIPE_PRICE_ID_YEARLY=price_1T31pcLdVnAwm3JUOBahIxyv

# URLs (update LANDING_BASE_URL after first deploy to get the Vercel domain)
LANDING_BASE_URL=https://YOUR_LANDING_DOMAIN.vercel.app
VITE_APP_URL=https://kainos-app-main.vercel.app
